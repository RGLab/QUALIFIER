#Generated by codetoolsBioC version 0.0.16
#Timestamp: Tue Mar  6 12:38:55 2012

#Imports: BiocGenerics, IDPmisc, MASS, RBGL, RSVGTipsDevice, XML,
#         flowCore, flowViz, flowWorkspace, grDevices, graphics, grid,
#         hwriter, lattice, methods, reshape, stats, utils
import(BiocGenerics)
importClassesFrom(flowCore, curv1Filter, curv2Filter,
                  logicalFilterResult, multipleFilterResult,
                  polygonGate, rectangleGate)

importClassesFrom(flowWorkspace, GatingHierarchy, GatingSet)

importClassesFrom(methods, ANY, character, environment, integer, list)

importMethodsFrom(XML, saveXML)

importMethodsFrom(flowCore, "%in%", assign, colnames, "colnames<-",
                  description, exprs, filter, filterDetails, get,
                  ls, ncol, nrow, pData, "pData<-", parameters, print,
                  "sampleNames<-", sort, split, varMetadata,
                  "varMetadata<-")

importMethodsFrom(flowViz, glpolygon, xyplot)

importMethodsFrom(flowWorkspace, getData, getGate, getNodes, getParent,
                  getPopStats, getSamples, lapply, show, summary)

importFrom(IDPmisc, IDPcolorRamp, col2hsv)

importFrom(MASS, huber, rlm)

importFrom(stats4, plot)

importFrom(RBGL, sp.between)

importFrom(RSVGTipsDevice, devSVGTips, setSVGShapeToolTip,
           setSVGShapeURL)

importFrom(XML, addChildren, replaceNodes, xmlCDataNode, xmlNode,
           xmlParse, xmlRoot)

importFrom(flowViz, flowViz.par.get, panel.densityplot.flowset)

importFrom(grDevices, colorRampPalette, densCols, dev.list, dev.off,
           png)

importFrom(graphics, abline, par)

#importFrom(grid, downViewport, gpar, grid.layout, grid.newpage,
#           grid.rect, grid.text, pushViewport, unit, upViewport,
#           viewport)
import(grid)

importFrom(hwriter, closePage, hwrite, openPage)

#importFrom(lattice, current.panel.limits, lattice.getOption,
#           lattice.options, packet.panel.default, panel.abline,
#           panel.fill, panel.grid, panel.linejoin, panel.lines,
#           panel.lmline, panel.loess, panel.points, panel.polygon,
#           panel.segments, panel.smoothScatter, panel.superpose,
#           panel.text, panel.xyplot, trellis.device, trellis.grobname,
#           trellis.par.get, trellis.par.set, trellis.vpname,which.packet,packet.number,
#           panel.number)

import(lattice)

importFrom(methods, "@<-", as, is, new)

#importFrom(reshape, cast, melt)

importFrom(stats, IQR, as.formula, dbeta, dt, mad, median, optim,
           pbeta, pnorm, pt)

importFrom(utils, read.csv)

importFrom(plyr, is.formula)

exportClasses(qaTask)

export( "saveToDB"
                ,"makeQaTask"
                ,"proportion.outliers.robust"
                ,"proportion.outliers.mle"
                ,"qoutlier"
                ,"outlier.cutoff"
                ,"outlier.t"
                ,"outlier.norm"
                ,rlm
                )


exportMethods("plot","qaCheck","getQAStats","qaReport","qpar","qpar<-")          

